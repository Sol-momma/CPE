#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--) {
        int r;
        cin >> r;

        vector<int> s(r);
        for (int i = 0; i < r; i++) cin >> s[i];

        // TODO: ここを実装する
        // 1. s をソートする
        // 2. 中央値を取る（s[r/2]）
        // 3. 各親戚との距離の合計を計算して出力する

    }

    return 0;
}
